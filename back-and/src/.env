# Porta do servidor
PORT=3000

# Banco de dados (exemplo para PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=seu_usuario
DB_PASS=sua_senha
DB_NAME=knuth_db

# JWT (se o projeto usa autenticação)
JWT_SECRET=uma_chave_super_secreta

# Redis (se estiver usando cache)
REDIS_HOST=localhost
REDIS_PORT=6379
